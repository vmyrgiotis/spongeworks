# SpongeWorks

Reinitialized repository for SpongeWorks.

## Setup

- Create/activate virtual environment with `uv`
- Install dependencies from `pyproject.toml`

## Run

```bash
uv run python main.py
```
